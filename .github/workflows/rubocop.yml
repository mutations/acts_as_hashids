name: Lint
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1.237.0
        with:
          ruby-version: 3.3
          bundler-cache: true
      - name: Install dependencies
        env:
          ACTIVE_RECORD_VERSION: 8.0
        run: bundle install --jobs 3 --retry 3
      - name: Run tests
        run: bundle exec rubocop -D

